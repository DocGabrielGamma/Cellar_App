const styles = theme => {
  return {
    paper: {
      margin: 'auto',
      width: 400,
      height: 400,
      backgroundColor: theme.palette.background.paper,
      padding: "10px",
      outline: "none",
      position: "absolute",
      top:0,
      bottom: 0,
      left: 0,
      right: 0
    }
  };
};

export default styles;
